# Potluck Web — CTA dock pass

Scope: hero CTA controls only. Everything below the hero is unchanged.

- Pre-spin keeps the approved full-width Potluck Spin control.
- Post-spin replaces it in the same dock position with See the recipe + the escalating reroll control.
- Share is removed from the hero.
- Counter now lives directly under the dock controls in every state.
- Primary and secondary post-spin controls use fully opaque surfaces.
- The dock has identical control height before and after reveal, so the CTA shelf does not jump when a recipe lands.
